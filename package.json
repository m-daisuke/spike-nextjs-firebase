{
  "name": "spike-nextjs-firebase",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "14"
  },
  "workspaces": [
    "common",
    "functions",
    "hosting"
  ],
  "scripts": {
    "build:dev": "yarn workspace common build && yarn workspace functions build && NODE_ENV=development yarn workspace hosting build",
    "build:prod": "yarn workspace common build && yarn workspace functions build:prod && NODE_ENV=production yarn workspace hosting build",
    "clear": "rm -rf common/dist functions/dist hosting/.next hosting/out",
    "serve": "firebase emulators:start --only functions,hosting",
    "deploy": "firebase deploy --only functions,hosting",
    "lint": "yarn workspace common lint && yarn workspace functions lint && yarn workspace hosting lint",
    "format": "yarn workspace common format && yarn workspace functions format && yarn workspace hosting format"
  }
}
