{
  "name": "spike-nextjs-firebase",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "14"
  },
  "workspaces": [
    "functions",
    "hosting"
  ],
  "scripts": {
    "build": "yarn workspace functions build && yarn workspace hosting build",
    "serve": "firebase emulators:start --only functions,hosting"
  }
}
